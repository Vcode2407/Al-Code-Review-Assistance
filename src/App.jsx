import React, { useState } from "react"; import axios from "axios"; import "./App.css"; function App() { const [code, setCode] = useState(""); const [review, setReview] = useState(""); const [loading, setLoading] = useState(false); const API_URL = "https://cuhpemmwjabfzkaithcm.supabase.co/functions/v1/ai-review"; const handleReview = async () => { if (!code) return alert("Paste code first!"); setLoading(true); setReview(""); try { const response = await axios.post(API_URL, { code }); setReview(response.data.review); } catch (e) { setReview("Error: " + e.message); } finally { setLoading(false); } }; return ( <div className="App"> <header className="App-header"> <h1>AI Code Reviewer 2026</h1> <p>Paste your JavaScript code below to get an instant AI security and logic audit.</p> <textarea value={code} onChange={(e) => setCode(e.target.value)} placeholder="function test() { eval('var x = 1'); }" /> <button onClick={handleReview} disabled={loading}>{loading ? "Analyzing..." : "Review Code"}</button> {review && <div className="res"><h3>AI Feedback:</h3><pre>{review}</pre></div>} </header> </div> ); } export default App;
